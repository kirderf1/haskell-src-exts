--{-# LANGUAGE TypeApplications #-}
--{-# LANGUAGE TypeOperators #-}
{-# LANGUAGE ComposableTypes #-}

piece Testa a = Add a a
-- data Test a = Add a a 

--data a :+: b = Proxy

--foo = id @(Int :+: Int)

--_ @> m = m
